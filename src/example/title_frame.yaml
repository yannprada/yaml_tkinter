TitleFrame:
    children:
        - Label:
            name: title_label
            text: Example
            font: ['', 20, bold]
            pack: left
        - Frame:
            pack:
                side: left
                expand: true
                fill: x
        - Button:
            text: Exit
            # this method must then be defined in the application class
            app_command: on_button_exit
            width: 5
            pack: left
    pack:
        side: top
        fill: x